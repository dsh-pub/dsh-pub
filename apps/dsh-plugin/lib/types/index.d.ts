/**
 * dsh.pub directory plugin, node half. The host entry is intentionally pure:
 * the bundle contributes only a browser Settings section.
 */
/** Host plugin body — no host-side behavior or third-party code execution. */
export declare function apply(): void;
